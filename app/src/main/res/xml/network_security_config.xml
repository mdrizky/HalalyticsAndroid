<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Halalytics
    - Blocks cleartext (HTTP) traffic globally for security
    - Allows cleartext to 10.0.2.2 (Android emulator loopback) for development only
    - In production, ALL connections must use HTTPS
-->
<network-security-config>
    <!-- Default: Block all cleartext traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext to emulator loopback for local Laravel dev server -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
